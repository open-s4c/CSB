kprobe:bpf_copy_from_user
__FILTER__
{
    @start[pid] = nsecs;
}

kretprobe:bpf_copy_from_user
/ @start[pid] /
{
    @bpf_copy_from_user_ns[pid, comm] = hist(nsecs - @start[pid]);
    delete(@start[pid]);
}

