tracepoint:irq:irq_handler_entry
__FILTER__
{
    @start[pid] = nsecs;
}

tracepoint:irq:irq_handler_exit
/ @start[pid] /
{
    @irq_handler_ns[pid, comm] = hist(nsecs - @start[pid]);
    delete(@start[pid]);
}

