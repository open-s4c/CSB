kprobe:__mem_cgroup_charge
__FILTER__
{
    @start[tid] = nsecs;
}
kretprobe:__mem_cgroup_charge
 /@start[tid]/
{
    $duration = (nsecs - @start[tid]) / 1000;
    @__mem_cgroup_charge_latency[pid, comm] = hist($duration);
    delete(@start[tid]);
}

