kprobe:queued_read_lock_slowpath
__FILTER__
{ @start[tid] = nsecs; }

kretprobe:queued_read_lock_slowpath
/ @start[tid] /
{
    @queued_read_lock_slowpath_ns[pid, comm] = hist(nsecs - @start[tid]);
    delete(@start[tid]);
}

